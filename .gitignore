node_modules/
dist/
*.log

# Only the root .env holds a private key, so only it stays out of the repo.
# The backtest and data-download configs are plain parameters and are committed.
/.env
.venv/
__pycache__/
.ipynb_checkpoints/
.claude/settings.local.json
